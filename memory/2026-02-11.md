# 2026-02-11

## Research: GitHub user `struong` (Steven Truong)

Requested by Sam. Findings:
- **Current role**: Engineer at **Tempo** (tempo.xyz) — L1 blockchain for payments, incubated by Stripe & Paradigm
- **Previous**: Stripe (crypto team), Ripple
- **Active work**: PRs to tempoxyz/examples, tempoxyz/docs, tempoxyz/tempo-go, adding Tempo to ethereum-lists/chains
- **Contact**: website struo.ng, Twitter @_struong, LinkedIn /in/struongs (private), GitHub struong, Are.na steven-truong
- **Background**: Deep payments/crypto infrastructure. Personal repos include ILP (Interledger) plugin, Rust GitHub pruner tool

## PRs Created

- **poncho#107**: Resizable handle border 1px→2px (`w-px`→`w-0.5`), assigned jasonhedman
- **poncho#112**: Fix drag crash — added `icons/32x32.png` to extraResources, wrapped startDrag in try/catch

## Issues Created

- **poncho#115**: Chat icon not loading (reported by shafu)
- **x402scan-mcp#7**: x402 fetch fails against Browserbase v1 — root cause: `x402-fetch.ts:42` uses `fromConfig()` which only registers v2 schemes, missing `x402Version: 1`

## In Progress

- **LiteLLM 400 error handling**: Branch `craig/handle-model-errors-gracefully` on poncho. `streamText()` in `chat.ts` ~line 300 has no error wrapping; raw LiteLLM JSON errors get piped to UI. Need to wrap error path and clean up stream buffer functions.
- **Alvaro's Windows installer space-in-username bug**: Awaiting clarification

## Repo Mappings

- `easy-agent` = `poncho` locally at `~/Code/merit-systems/poncho` (default branch: `master`)
- `x402scan-mcp` monorepo is now `agent-cash` at `~/Code/merit-systems/agent-cash`

## Misc

- Diagnosed macOS DMG mount failure for Annie Collins as MDM/machine-level issue
- Poncho was briefly down (~14:47-14:57 UTC) due to LiteLLM virtual key loading; rsproule fixed by restarting
